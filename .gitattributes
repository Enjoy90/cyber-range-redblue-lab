# Project ini di-deploy di Linux (Docker / Proxmox).
# Paksa line ending LF agar script shell & file config tidak rusak oleh
# konversi CRLF Windows (mencegah error "$'\r': command not found").
* text=auto eol=lf
*.sh    text eol=lf
*.py    text eol=lf
*.conf  text eol=lf
Dockerfile text eol=lf
